<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SmallLabyWpfPlayer">
    <DataTemplate DataType="{x:Type local:InfoViewModel}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Gold"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Gold}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Speed"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Speed}"/>
        </Grid>
    </DataTemplate>
</ResourceDictionary>